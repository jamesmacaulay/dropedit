// Bundled starter projects (imported ?raw to preserve exact Drop formatting).
// CLEAN_INIT is the blank-slate state shown on first load with no saved project.
import cleanInit from './clean-init.json?raw'
import dawInit from './daw-init.json?raw'

export const CLEAN_INIT = cleanInit
export const DAW_INIT = dawInit
